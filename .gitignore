# Build output
bin/
obj/
*.user
# IDE
.vs/
.idea/
# OS
Thumbs.db
.DS_Store
# Raw media & model sources (keep out of repo; use external/LFS if needed)
*.mp4
*.mp3
*.blend
*.blend1
# Dated screenshots / recordings. These are the default file names a Japanese
# Windows produces, so the patterns have to stay in Japanese to match.
スクリーンショット*.png
レコーディング*
画像*.png

# Claude Code / superpowers workflow artifacts - not part of the published mod
.superpowers/
